ktor {
  deployment {
    port = 80
    port = ${?PORT}
  }
  application {
    modules = [ com.denchic45.financetracker.ApplicationKt.module ]
  }
  environment = "test"
}

jwt {
  audience = ${JWT_AUDIENCE}
  secret = ${JWT_SECRET}
}